# Dependency Review Configuration
# Excludes GitHub Actions from license checks (CI-only, not distributed)

# Exclude all GitHub Actions from license review
# Actions are development/CI tools, not runtime dependencies
exclude-paths:
  - ".github/workflows/**"

# Allow lower scorecard for development tools
scorecard-threshold: 2.5
